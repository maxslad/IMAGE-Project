pip install numpy
pip install opencv-python
pip install cmake
pip install dlib
pip install mediapipe
pip install PyAutoGUI

python method2.py

วิธีใช้งาน
กด s เพื่อ cablibrate โดยให้มองไปที่มุม ซ้าย ขวา บน ล่าง ของจอตามลำดับ
ทุกครั้งที่มองให้กดปุ่ม spacebar เพื่อยืนยันแต่ละมุม หากมองผิดให้ กด a หรือ backspace
เพื่อทำการย้อนกลับไป calibrate มุมก่อนหน้า
กด esc หรือ q เพื่อทำการออกโปรแกรม
